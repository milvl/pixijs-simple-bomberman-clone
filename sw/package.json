{
  "name": "uur_bomberman_clone",
  "version": "1.0.0",
  "description": "A semestral work for KIV/UUR",
  "scripts": {
    "quickstart": "http-server",
    "clean": "rm -rf dist",
    "start": "npm run clean && mkdir dist && cp -r img dist && cp -r audio dist && cp -r fonts dist && parcel index.html",
    "build": "npm run clean && mkdir dist && cp -r img dist && cp -r audio dist && cp -r fonts dist && parcel build index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Milan Vlachovsky (mvlach@students.zcu.cz)",
  "license": "MIT",
  "dependencies": {
    "@pixi/sound": "^6.0.0",
    "jquery": "^3.7.1",
    "pixi.js": "^8.1.1"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "parcel": "^2.12.0"
  }
}
